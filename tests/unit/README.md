# Unit tests

PagePool, BlockTable, COW and reference-count tests are being migrated here.
Existing flat test modules remain discoverable while the directory migration is
completed without changing test semantics.

# Qualification tests

Physical-GPU and real-checkpoint qualification is executed by the tools under
`tools/`; raw outputs are CI/release artifacts, while summaries are tracked in
`real_results/kv_v2/`.
